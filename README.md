# C-AI-
基于 C++ muduo 网络库实现高性能集群聊天服务器，采用网络层、业务层、数据层解耦设计，支持注册登录、好友管理、单聊、群聊、离线消息、Redis 跨服务器消息转发和 Nginx TCP 负载均衡。在此基础上扩展 RAG 语义问答模块，将群聊历史消息异步写入向量检索服务，实现聊天记录语义搜索、群聊上下文问答、未读消息摘要和群聊 AI 助手功能。通过 AI Gateway 将大模型调用、Embedding 生成、向量检索与 C++ 主通信服务解耦，降低 AI 推理链路对实时消息投递延迟的影响。
